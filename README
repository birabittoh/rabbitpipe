rabbitpipe
----------

My own custom wrapper for the Invidious API.


FEATURES

• Get video download links.
• Get search results.
• Automatically change instance if the current one's failing.
• Built-in in-memory cache!


INSTALLING

`go get github.com/birabittoh/rabbitpipe`


USAGE

`rp := rabbitpipe.New("")` creates a new rabbitpipe object; you can
also set a custom starting instance.

`rp.GetVideo("dQw4w9WgXcQ")` gets all info for the requested video,
including its formats.

`rp.Search("search query")` returns the search results that match
the provided search query.


LICENSE

rabbitpipe is licensed under MIT.
